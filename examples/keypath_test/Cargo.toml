[package]
name = "keypath_test"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
iced = { path = "../.." }
# Note: Once keypath feature is implemented in iced, use:
# iced = { path = "../..", features = ["keypath"] }
tagged-core = "0.7.0"
key-paths-core = { version = "1.3.0", features = ["tagged_core"] }
key-paths-derive = "1.0.5"

